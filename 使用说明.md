# 📁 网站内容替换指南

## 文件结构

```
personal-website/
├── index.html          ← 主页面（编辑标题、描述、个人信息）
├── styles.css          ← 样式（修改颜色、字体、间距）
├── script.js           ← 交互脚本（一般无需修改）
└── assets/
    ├── covers/         ← 🖼 封面图片放这里
    │   ├── work-01-cover.jpg
    │   ├── work-02-cover.jpg
    │   └── ...
    └── videos/         ← 🎬 视频文件放这里
        ├── work-01.mp4
        ├── work-02.mp4
        └── ...
```

---

## 🎬 如何上传你的视频

### 方案一：本地视频文件（推荐）

1. **把视频文件放到 `assets/videos/` 目录**
   - 命名建议：`work-01.mp4`、`work-02.mp4`...
   - 支持格式：MP4（推荐）、WebM、MOV

2. **制作封面图放到 `assets/covers/` 目录**
   - 命名建议：`work-01-cover.jpg`、`work-02-cover.jpg`...
   - 推荐尺寸：1280×720（16:9）
   - 支持格式：JPG、PNG、WebP

3. **编辑 `index.html`，找到对应的作品卡片，修改两个属性：**

```html
<!-- 示例：修改第一个作品 -->
<div class="work-card" data-category="media"
     data-cover="assets/covers/我的封面.jpg"        ← 改成你的封面路径
     data-video="assets/videos/我的视频.mp4">        ← 改成你的视频路径
```

4. **同时修改卡片内的文字：**
```html
<div class="work-card-info">
    <span class="work-card-category">自媒体</span>           ← 分类标签
    <h3 class="work-card-title">你的作品标题</h3>            ← 作品名
    <p class="work-card-desc">简短描述...</p>                ← 一句话介绍
    <div class="work-card-tags">
        <span>标签1</span><span>标签2</span>                 ← 标签
    </div>
</div>
```

---

### 方案二：使用在线视频（B站/YouTube）

只需把 `data-video` 改成视频链接即可，封面图仍然放在本地：

```html
<div class="work-card" data-category="media"
     data-cover="assets/covers/my-cover.jpg"
     data-video="https://www.bilibili.com/video/BVxxxxxxxxx">   ← B站链接
```

支持的外链格式：
- B站：`https://www.bilibili.com/video/BVxxxxx`
- YouTube：`https://www.youtube.com/watch?v=xxxxx` 或 `https://youtu.be/xxxxx`
- 其他外链：直接写完整URL

---

### 方案三：仅显示封面，不播放视频

如果某个作品暂时只有封面没有视频，把 `data-video` 删掉或留空：

```html
<div class="work-card" data-category="media"
     data-cover="assets/covers/my-cover.jpg">   ← 没有 data-video，点击不会弹窗
```

---

## 👤 修改个人信息

在 `index.html` 中搜索并替换以下内容：

| 搜索关键词 | 替换为 |
|-----------|--------|
| `张创作` | 你的姓名/品牌名 |
| `hello@creator.com` | 你的邮箱 |
| `creator_zhang` | 你的微信号 |
| `138-0000-0000` | 你的手机号 |
| `CR` (导航栏logo) | 你的品牌缩写 |

---

## 🎨 修改配色

打开 `styles.css`，在文件顶部找到 `:root` 块：

```css
:root {
    --color-accent: #2c3e6b;       /* 主强调色（深蓝） */
    --color-gold: #c4a962;         /* 金色点缀 */
    --color-bg: #fafaf9;           /* 背景色 */
    ...
}
```

修改这些颜色值即可全局生效。

---

## ➕ 新增作品

**方式一：复制粘贴现有卡片**

在 `index.html` 的 `<div class="works-grid">` 内，复制一个已有的 `work-card` 块，修改：
- `data-category`：`media` / `corporate` / `ai`
- `data-cover` 和 `data-video` 路径
- 标题、描述、标签

**方式二：点击"加载更多"按钮**

页面上已有「加载更多」按钮，点击后会自动添加3张新卡片（演示用）。你可以修改 `script.js` 中 `loadMoreBtn` 部分的模板数据来定制新增内容。

---

## 🚀 部署上线

这是纯静态网站，可以部署到任意平台：

- **GitHub Pages**：推送到 GitHub 仓库，开启 Pages 即可
- **Vercel / Netlify**：拖拽 `personal-website` 文件夹即可部署
- **自有服务器**：上传整个文件夹到网站根目录

⚠ 注意：视频文件较大时，建议使用外链（B站/YouTube）或 CDN 加速。

---

## ❓ 常见问题

**Q：封面图不显示？**
- 检查图片路径是否正确（区分大小写）
- 确认图片已放入 `assets/covers/` 目录
- 打开浏览器开发者工具 → Console 查看具体错误

**Q：视频点不开？**
- 确认 `data-video` 属性已设置
- 本地视频需先放到 `assets/videos/` 目录
- 外链视频确保链接完整且可公开访问

**Q：手机上看视频太小？**
- 视频弹窗会自动适配屏幕，PC和手机均可全屏
- 封面图建议使用 16:9 比例
